import {Episodes} from "../components/EpisodeContainer/Episodes";
import {useLocation} from "react-router-dom";

const EpisodePage = () => {
       return (
        <div>
            <Episodes/>
        </div>
    );
};

export {EpisodePage};