const baseURL = 'https://jsonplaceholder.typicode.com';

const comments='/comments'
const posts='/posts'

const urls={
    base:{
        comments:comments,
        posts:posts
    }
}

export {baseURL,urls}